const { isPalindrome } = require('../index');

const testArray = [1, 2];

describe('Is Palindrome Linkedlist', () => {
  it('Should check if its is plaindrome linklist ', () => {
    // expect(isPalindrome(testArray)).toBeTrutly();
  });
});
