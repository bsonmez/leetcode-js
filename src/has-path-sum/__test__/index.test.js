/* eslint-disable no-undef */
const { hasPathSum } = require('../index');

describe('Has Path Sum', () => {
  it('Should calculate path sum ', () => {
    expect(hasPathSum(sum = 22)).toBeTruthy();
  });
});
